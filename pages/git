git

- switch between branches
    git switch <branch>

- create a new branch
    git checkout -b <new-branch>

- make a pull request
    git push -u origin <branch> (search notes pull-request for more)

- display untracked files
	git status

- search for string a file
	git grep <string> -- <file-path>

- rename or move a file
	git mv old.c new.c

- update local repo to match remote repo
	git pull origin main

- move the current commit to a previous commit
	git reset --hard 31f2bb1

- set up a new project
	git init
	git commit -m 'first commit'
	git branch -M main	# define branch

	- if remote repo
	git remote add <URL>.git

	git push -u origin main
